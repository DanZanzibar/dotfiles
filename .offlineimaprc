[general]
accounts = work, personal, ctc, lndc
pythonfile = ~/.offlineimap/.helper.py

[Account work]
localrepository = Local-work
remoterepository = Remote-work

[Repository Local-work]
type = Maildir
localfolders = ~/.mail/work/

[Repository Remote-work]
type = IMAP
remotehost = outlook.office365.com
remoteusereval = get_work_username()
sslcacertfile = /etc/ssl/certs/ca-certificates.crt
auth_mechanisms = XOAUTH2
oauth2_client_id_eval = get_client_id()
oauth2_client_secret_eval = get_client_secret()
oauth2_refresh_token_eval = get_refresh_token()
oauth2_request_url = https://login.microsoftonline.com/4a8c66e5-120a-4192-a8b9-83c66d812331/oauth2/v2.0/token
folderfilter = lambda folder: folder in ['INBOX', 'Archive', 'Sent Items', 'Templates', 'Trash']

[Account personal]
localrepository = Local-personal
remoterepository = Remote-personal

[Repository Local-personal]
type = Maildir
localfolders = ~/.mail/personal

[Repository Remote-personal]
type = IMAP
remotehost = imap.gmail.com
remoteusereval = get_username('personal')
remotepasseval = get_password('personal')
sslcacertfile = /etc/ssl/certs/ca-certificates.crt
auth_mechanisms = PLAIN
folderfilter = lambda folder: folder in ['INBOX', 'Archives', '[Gmail]/Sent Mail', '[Gmail]/Trash', 'Templates']

[Account ctc]
localrepository = Local-ctc
remoterepository = Remote-ctc

[Repository Local-ctc]
type = Maildir
localfolders = ~/.mail/ctc

[Repository Remote-ctc]
type = IMAP
remotehost = imap.gmail.com
remoteusereval = get_username('ctc')
remotepasseval = get_password('ctc')
sslcacertfile = /etc/ssl/certs/ca-certificates.crt
auth_mechanisms = PLAIN
folderfilter = lambda folder: folder in ['INBOX', 'Archives', '[Gmail]/Sent Mail', '[Gmail]/Trash', 'Templates']

[Account lndc]
localrepository = Local-lndc
remoterepository = Remote-lndc

[Repository Local-lndc]
type = Maildir
localfolders = ~/.mail/lndc

[Repository Remote-lndc]
type = IMAP
remotehost = imap.gmail.com
remoteusereval = get_username('lndc')
remotepasseval = get_password('lndc')
sslcacertfile = /etc/ssl/certs/ca-certificates.crt
auth_mechanisms = PLAIN
folderfilter = lambda folder: folder in ['INBOX', 'Archives', '[Gmail]/Sent Mail', '[Gmail]/Trash', 'Templates']
